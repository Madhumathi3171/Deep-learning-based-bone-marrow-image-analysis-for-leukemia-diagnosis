num_classes = len(train_generator.class_indices)
model = DC_CBAM_InceptionV3(input_shape=(128,128,3), num_classes=num_classes)
model.compile(optimizer='adam',
              loss='categorical_crossentropy',
              metrics=['accuracy'])

model.summary()
